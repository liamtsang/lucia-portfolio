---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Posts from '../components/Posts.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			p {
				font-size: 4rem;
				font-family: 'Minion Pro Italic';
				font-style: italic;
			}
			dialog {
				position: fixed;
				top: 0;
				left: 0;
				width: 100vw;
				height: 100%;
				background-color: rgba(0,0,0,0);
				backdrop-filter: blur(50px);
				border: none;
				margin: 0;
				display: none;
				transition: display 0.5s ease;
			}
			dialog p {
				font-family: 'Authentic Sans';
				font-style: normal;
				width: 50vw;
				padding-left: 5vw;
				font-size: 1.5rem;
			}

			body {
				padding-left: 5vw;
				padding-right: 5vw;
			}
			.animation {
				z-index: 5;
				position: fixed;
				right: -5rem;
				top: -5rem; 
				width: 630px;
				height: 810px;
				background: url('spritesheet.png') left center;
				background-size: cover;
				mix-blend-mode: multiply;

				animation: play 1.5s steps(22) infinite;
				animation-play-state: paused;
				animation-delay: calc(var(--scroll) * -.0025s);
			}
			h1 {
				font-size: 6rem;
			}
			@media screen and (max-width: 768px) {
				main {
					padding: 0;
					padding-top: 2rem;
				}
				.animation {
					width: 236.25px;
					height: 303.75px;
					right: -1.25rem;
					top: 34rem;
					animation: playMobile 1.5s steps(22) infinite;
					animation-play-state: paused;
					animation-delay: calc(var(--scroll) * -.0025s);
				}
				h1 {
					font-size: 3rem;
				}
				p {
					font-size: 2rem;
				}
				dialog p {
					font-family: 'Authentic Sans';
					font-style: normal;
					width: 80vw;
					padding-left: 5vw;
					font-size: 1rem;
				}
			}
			@keyframes play {
				100% {
					background-position: 13860px;
				}
			}
			@keyframes playMobile{
				100% {
					background-position: 5197.5px;
				}
			}
		</style>
		
	</head>
	<body>
		<div class="animation" onclick="toggleDialog()"></div>
		<dialog id="dialog">
			<p>
				I am a Filipino-American creator, and I grew up in Hawai’i on the island of O’ahu. My current art practice is fabrication. It combines everything I am interested in, the art of process, iterating on ideas and prototypes, working with new material, but mostly I loved that I was able to make something I could really hold. Being exposed to more machines such as the CNC router, 3D printing, laser cutting, and the Risograph, creates a flow of ideas that feels right.
			</p>
	
		</dialog>
		<main>
			<h1>Lucia Mumma</h1>
			</div>
			<p>
				is an artist and fabricator.			</p>
			<p>
				they design, cut, and assemble in all the ways you can. 
			</p>
			
		</main>
		<Posts />
		<Footer />
		<script defer>
			const dialog = document.querySelector("#dialog");
			let isDialogOpen = false;

			function toggleDialog() {
				if (isDialogOpen) {
					dialog.style.display = "none"
					isDialogOpen = false
				} else {
					dialog.style.display = "block"
					isDialogOpen = true
				}
			}

			window.addEventListener('scroll', () => {
				document.body.style.setProperty('--scroll', window.scrollY.toString());
			}, false);
		</script>
	</body>
</html>
